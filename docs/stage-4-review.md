# 階段 4：完整內容搬遷

## 完成範圍

- Notebook：再次核對 ZIP 中 76 份原始文件與階段 1 盤點 SHA-256，全部吻合。75 份公開（72 篇文章、3 份介紹），1 份原有草稿保留但不發布。149 個去重後的舊站圖片資產已搬入，引用圖片的位元組檢查通過。
- 所有舊文 `publishedAt`／`updatedAt` 維持空白。不猜測日期；Latest Notes 仍顯示空狀態，未來新增有日期文章後由建置程序挑選。
- 完整 Notebook 目錄、搜尋、系列順序、文章目錄及手機抽屜沿用確認後版本。
- Work：開源 1、專利 3、講課 1、競賽 1、讀書會／分享 34、工作 3，共 43 筆。原有文字、可見日期、連結和順序保留；原 TSX 註解中的項目沒有發布。
- 每個 Work 分類獨立 Markdown，透過共用編譯器於建置時產生 HTML。保留原版淡灰白底、字體、細分隔線，增加分類錨點捷徑。寬螢幕兩欄；長清單各佔完整區段、內部兩欄，避免鄰欄大片留白；小於 900px 全部單欄。
- 75 個公開舊文章路徑及 `/docs/` 入口皆有靜態轉接頁。保留 query/hash，使用 replace 避免返回鍵陷入轉接迴圈；無 JavaScript 時提供連結。原文中指向舊站 `/docs/` 的已知連結，在渲染時轉為新版 Notebook URL，Markdown 原連結保留於來源。

## 驗證

- `pnpm test`：9 項通過。包含 76 份舊文還原後的原文雜湊、圖片位元組、日期、完整導覽、最新排序、Markdown 格式、43 筆 Work 的逐筆文字及連結、舊路徑與公開文章的一對一對照。
- `pnpm build`：Next.js SSG 成功，159 個 HTML 檔案；10,449 個站內連結、資源及錨點檢查通過。包含所有公開舊路徑的 fallback 連結、草稿路徑不輸出、Work 實際輸出 43 筆與講課連到新 Notebook 的斷言。
- Next.js 建置內的 TypeScript 檢查通過；另執行 `pnpm typecheck` 確認測試檔型別。
- 本次未執行瀏覽器互動／截圖驗證；RWD 與轉接的實際視覺體驗供本輪預覽確認。

## 邊界

轉接為靜態相容頁，不是 HTTP 301/308。歷史 hash 原樣保留，未建立所有歷史標題 ID 別名。外部簡報、影片及專利連結保留原址，未驗證第三方登入權限與可用性。

26 篇獨立 Blog 不在此次範圍。Home／Contact 正式內容、正式 SEO、GitHub Actions 與 main／GitHub Pages 切換仍屬階段 5，待確認後進行。
